[project]
name = "entropy-cascade-manager"
authors = []
description = ""
telemetry = true
requirements = []
cache_dir = "/home/runner/workspace/entropy-cascade-manager/./.requirements"
boot_contracts = ["pox", "costs-v2", "bns"]
[contracts.entropy-cascade-manager]
path = "contracts/entropy-cascade-manager.clar"

[repl]
costs_version = 2
parser_version = 2

[repl.analysis]
passes = ["check_checker"]

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false
